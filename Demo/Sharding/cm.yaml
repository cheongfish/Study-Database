apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-sql
data:
  init.sql: |
    create table URL_TABLE
    (
        id serial Not Null Primary Key,
        url text,
        url_id character(5)
    ) ;
